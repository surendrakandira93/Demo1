@model  List<Demo1.Models.Department>
@using Demo1.Models
@{
    ViewBag.Title = "Home Page";
}

<main>
    <section class="row" aria-labelledby="aspnetTitle">
        <p><a href="/Department/addedit" class="btn btn-primary btn-md">Create</a></p>
    </section>

    <div class="row">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Name</td>
                     
                        <td>
                            <a href="/Department/addedit/@item.Id" class="btn btn-primary btn-md">Edit</a>
                            <a href="/Department/delete/@item.Id" class="btn btn-danger btn-md">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</main>